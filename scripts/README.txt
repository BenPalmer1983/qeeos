Wrapper script for pw.x

Problem:    some nodes were running out of scratch space
Solution:   a) either use a larger scratch
            b) or use wrapper script pw.sh that clears scratch after each execution of pw.x
